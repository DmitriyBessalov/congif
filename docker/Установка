sudo apt update
sudo apt install apt-transport-https ca-certificates curl gnupg-agent software-properties-common
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
sudo apt update
sudo apt install docker-ce docker-ce-cli docker-compose containerd.io

# Проверка
sudo systemctl status docker


----------------------------------
# Запуск контейнера при отсутсвии подгружается из dockerhub
sudo docker run -it python:latest bash

# список контейнеров
docker ps




# docker exec -it {{CONTAINER_ID}} bash






-----------------------------------
Dockerfile 

apt-get update && apt-get upgrade -y




-----------------------------------
# Для подключения через Pycharm
sudo chmod 777 /var/run/docker.sock

Settings -> Docker -> + -> Docker -> Unix socket
