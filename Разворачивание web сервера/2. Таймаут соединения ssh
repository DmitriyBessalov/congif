#На клиентской машине чтобы соединение не обрывалось при простое:
sudo nano /etc/ssh/ssh_config
#Добавить строки в конец файла
    ServerAliveInterval 60
    ServerAliveCountMax 10
    
#Обязательно сохранить папку ~/.ssh/ на ноутбе чтобы не потерять доступ к серверу
#лучше сохранить содержимое файлов в заметках lastpass
